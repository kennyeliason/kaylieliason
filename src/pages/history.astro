---
const studyUnits = [
  {
    id: 'unit-1',
    shortLabel: 'Unit One',
    title: 'First Peoples + Societies',
    description: 'Study early humans, agriculture, domestication, and the first societies.',
    questions: [
  {
    category: 'Vocabulary',
    question: 'What is an artifact?',
    choices: ['An object of historical value made by human beings', 'A wild plant gathered by early people for food', 'A permanent settlement built beside a river', 'A migration path followed by moving groups'],
    answer: 'An object of historical value made by human beings',
    explanation: 'Artifacts are human-made objects that help historians understand the past.',
  },
  {
    category: 'Vocabulary',
    question: 'What does migrate mean?',
    choices: ['To move from one region to another', 'To tame wild animals for farming work', 'To build a village in one location', 'To store extra goods for later use'],
    answer: 'To move from one region to another',
    explanation: 'Migration means people move from one place to another.',
  },
  {
    category: 'Vocabulary',
    question: 'What is a hunter-gatherer?',
    choices: ['A person who survives by hunting game and gathering wild plants', 'A person who grows only wheat for a village', 'A leader who organizes work in a farming village', 'A seller who trades tools from place to place'],
    answer: 'A person who survives by hunting game and gathering wild plants',
    explanation: 'Hunter-gatherers found food in nature instead of raising it through farming.',
  },
  {
    category: 'Vocabulary',
    question: 'What does it mean to adapt?',
    choices: ['To develop characteristics that aid in survival', 'To leave a region forever and never return', 'To invent bronze tools for daily use', 'To stop moving and remain in one place'],
    answer: 'To develop characteristics that aid in survival',
    explanation: 'Adaptation helps living things survive in changing conditions.',
  },
  {
    category: 'Vocabulary',
    question: 'What is agriculture?',
    choices: ['The cultivation of plant foods and the domestication of animals', 'The study of old objects left by earlier people', 'The melting of glaciers during climate change', 'The exchange of only food between families'],
    answer: 'The cultivation of plant foods and the domestication of animals',
    explanation: 'Agriculture means farming plants and raising animals.',
  },
  {
    category: 'Vocabulary',
    question: 'What is domestication?',
    choices: ['The taming of wild animals or controlled cultivation of plants', 'The movement of humans between regions over time', 'The building of roads between early settlements', 'The storage of grain for future seasons'],
    answer: 'The taming of wild animals or controlled cultivation of plants',
    explanation: 'Domestication is the process of bringing plants and animals under human control.',
  },
  {
    category: 'Vocabulary',
    question: 'What is an economy?',
    choices: ['The system of producing and obtaining goods', 'A type of settlement with homes and fields', 'A kind of crop planted during the spring', 'A tool made from bone and sharpened stone'],
    answer: 'The system of producing and obtaining goods',
    explanation: 'An economy is how people make, trade, and use goods.',
  },
  {
    category: 'Vocabulary',
    question: 'What is a surplus?',
    choices: ['A supply of goods or labor not needed for short-term survival', 'A shortage of crops after a hard season', 'A wild animal hunted by early people', 'A kind of migration caused by weather'],
    answer: 'A supply of goods or labor not needed for short-term survival',
    explanation: 'A surplus means there is extra beyond what is needed right away.',
  },
  {
    category: 'Neolithic Revolution',
    question: 'What was the Neolithic Revolution also called?',
    choices: ['The Agricultural Revolution', 'The Ice Revolution of the ancient world', 'The Tool Revolution that changed metalwork', 'The River Revolution centered on waterways'],
    answer: 'The Agricultural Revolution',
    explanation: 'The Neolithic Revolution is another name for the Agricultural Revolution.',
  },
  {
    category: 'Neolithic Revolution',
    question: 'What major change happened during the Neolithic Revolution?',
    choices: ['Humans shifted from hunting and gathering to agriculture and permanent settlements', 'Humans stopped using animals to meet daily needs', 'People left river valleys for colder regions', 'All groups became nomadic and avoided settlements'],
    answer: 'Humans shifted from hunting and gathering to agriculture and permanent settlements',
    explanation: 'This was the big change that reshaped early societies.',
  },
  {
    category: 'Neolithic Revolution',
    question: 'Why were societies able to support larger populations after people began farming and living in permanent settlements?',
    choices: ['Agriculture produced a steadier food supply', 'People hunted more game than ever before', 'Everyone moved less and traded less often', 'Glaciers returned and improved growing seasons'],
    answer: 'Agriculture produced a steadier food supply',
    explanation: 'Farming helped feed more people more consistently.',
  },
  {
    category: 'Climate and Change',
    question: 'Why were climate changes around 12,000 BC important?',
    choices: ['The retreat of glaciers changed environments and encouraged agriculture', 'They ended all migration between ancient regions', 'They created bronze tools for farming societies', 'They made all animals tame around humans'],
    answer: 'The retreat of glaciers changed environments and encouraged agriculture',
    explanation: 'Climate change forced people to adjust how they got food.',
  },
  {
    category: 'Climate and Change',
    question: 'Why did agriculture become more important as game decreased?',
    choices: ['People needed a more reliable way to get food', 'Farming required less work than every other job', 'Animals disappeared everywhere across the world', 'Villages banned hunting in all nearby lands'],
    answer: 'People needed a more reliable way to get food',
    explanation: 'As hunting became less dependable, farming became more valuable.',
  },
  {
    category: 'Plants',
    question: 'Which two early grains were growing wild before being domesticated?',
    choices: ['Wild barley and wheat', 'Rice and corn grown in wet fields', 'Cotton and flax used mostly for cloth', 'Cassava and millet grown in warmer climates'],
    answer: 'Wild barley and wheat',
    explanation: 'The notes point to wild barley and wheat as early domesticated grains.',
  },
  {
    category: 'Plants',
    question: 'How did people improve crop yields and disease resistance over time?',
    choices: ['They used the best grains and mixed different varieties', 'They planted farther from rivers and water sources', 'They used only wild seeds with no selection', 'They stopped farming in groups and worked alone'],
    answer: 'They used the best grains and mixed different varieties',
    explanation: 'Selecting strong plants helped improve crops over time.',
  },
  {
    category: 'Settlements',
    question: 'Why did some nomadic groups choose to settle down?',
    choices: ['Agriculture made permanent communities more practical', 'They lost the ability to hunt for food', 'Animals forced them to stay in one place', 'Trade became illegal between neighboring groups'],
    answer: 'Agriculture made permanent communities more practical',
    explanation: 'Farming encouraged people to stay in one place.',
  },
  {
    category: 'Animals',
    question: 'How were animals domesticated?',
    choices: ['Through the ways they interacted with humans', 'By being released into forests near villages', 'By mixing them with crops in farming fields', 'Through bronze tools used by early farmers'],
    answer: 'Through the ways they interacted with humans',
    explanation: 'Animals that worked well around people were more likely to be domesticated.',
  },
  {
    category: 'Animals',
    question: 'Which animal was tamed as early as 12,000 BC?',
    choices: ['Dogs living closely with human groups', 'Cattle raised for milk and heavy labor', 'Goats kept for milk and meat in herds', 'Pigs fed near settlements for later use'],
    answer: 'Dogs living closely with human groups',
    explanation: 'Dogs were among the earliest domesticated animals.',
  },
  {
    category: 'Animals',
    question: 'Which animals were first domesticated between 8500 and 7000 BC?',
    choices: ['Sheep, goats, and pigs', 'Dogs, horses, and chickens kept for transport', 'Cattle, camels, and llamas used as larger animals', 'Wolves, deer, and foxes left as wild animals'],
    answer: 'Sheep, goats, and pigs',
    explanation: 'Those three are specifically listed for that period.',
  },
  {
    category: 'Animals',
    question: 'When were horned cattle not tamed until?',
    choices: ['About 6500 BC during later domestication', 'About 12,000 BC during very early settlement', 'About 8500 BC during early herding changes', 'About 3000 BC during later ancient empires'],
    answer: 'About 6500 BC during later domestication',
    explanation: 'Horned cattle were domesticated later than several other animals.',
  },
  {
    category: 'Animal Uses',
    question: 'What did animals provide besides meat?',
    choices: ['Milk, hides, wool, and manure that enriched the soil', 'Only bones for decoration in sacred spaces', 'Only transportation across oceans and seas', 'Silver and tools taken from the ground'],
    answer: 'Milk, hides, wool, and manure that enriched the soil',
    explanation: 'Animals were useful for many products beyond meat.',
  },
  {
    category: 'Animal Uses',
    question: 'What were horns and bones used for?',
    choices: ['They were carved to make tools', 'They were planted as crops in prepared fields', 'They were used to track glaciers across regions', 'They were traded for wheat only in markets'],
    answer: 'They were carved to make tools',
    explanation: 'Animal parts could be shaped into useful tools.',
  },
  {
    category: 'Pastoralism',
    question: 'What is pastoralism?',
    choices: ['A way of life based on herding and grazing animals', 'A way of life based only on fishing boats', 'A farming system based only on wheat fields', 'A method of writing laws for governments'],
    answer: 'A way of life based on herding and grazing animals',
    explanation: 'Pastoralism centers on raising and moving with herds.',
  },
  {
    category: 'Regional Impact',
    question: 'Which crops became important in Africa as agriculture spread?',
    choices: ['Cassava, bananas, palm nuts, and millet', 'Wheat, rye, and oats grown in cooler climates', 'Corn, beans, and squash grown in Mesoamerica', 'Olives, grapes, and figs grown near the Mediterranean'],
    answer: 'Cassava, bananas, palm nuts, and millet',
    explanation: 'Those are the crops named for Africa.',
  },
  {
    category: 'Regional Impact',
    question: 'Which crops are listed for the Americas?',
    choices: ['Maize, manioc, and sweet potatoes', 'Barley, wheat, and rice grown in Afro-Eurasia', 'Cotton, flax, and millet used for fiber and grain', 'Goats, pigs, and sheep raised as animals'],
    answer: 'Maize, manioc, and sweet potatoes',
    explanation: 'Those are the crops identified for the Americas.',
  },
    ],
    jeopardyCategories: [
      { name: 'Vocabulary', indexes: [0, 1, 2, 3, 4] },
      { name: 'Revolution', indexes: [5, 8, 9, 10, 12] },
      { name: 'Plants', indexes: [6, 7, 13, 14, 23] },
      { name: 'Animals', indexes: [16, 17, 18, 19, 20] },
      { name: 'Societies', indexes: [11, 15, 21, 22, 24] },
    ],
  },
  {
    id: 'unit-2',
    shortLabel: 'Unit Two',
    title: 'Ancient World Civilizations',
    description: 'Review the major civilizations, empires, inventions, governments, religions, and trade networks from Unit 2.',
    questions: [
      {
        category: 'Civilization Features',
        question: 'Which feature of civilization happened when settlements grew larger because farmers had extra food?',
        choices: ['Cities', 'Complex religions', 'Art and architecture', 'Public works'],
        answer: 'Cities',
        explanation: 'Surplus food helped populations grow, and some settlements turned into cities.',
      },
      {
        category: 'Civilization Features',
        question: 'What does job specialization mean in a civilization?',
        choices: ['People focus on different specific tasks instead of everyone doing all the work', 'Every person farms the same crop in the same way', 'People stop trading with nearby communities', 'Only kings and priests are allowed to work'],
        answer: 'People focus on different specific tasks instead of everyone doing all the work',
        explanation: 'Specialized jobs included work like teaching, scribing, stone-cutting, craft work, and building.',
      },
      {
        category: 'Civilization Features',
        question: 'Why did civilizations need organized central governments?',
        choices: ['To manage food supply, irrigation, clean water, and waste removal', 'To stop people from building cities', 'To make sure every person had the same job', 'To replace all forms of religion'],
        answer: 'To manage food supply, irrigation, clean water, and waste removal',
        explanation: 'Large communities needed leaders to organize systems that helped everyone survive.',
      },
      {
        category: 'Civilization Features',
        question: 'Why did early civilizations need writing?',
        choices: ['To record trade goods, food storage, and complex ideas', 'To replace spoken language completely', 'To stop people from practicing religion', 'To make farming unnecessary'],
        answer: 'To record trade goods, food storage, and complex ideas',
        explanation: 'Writing helped civilizations keep track of information that was too complicated to remember or draw.',
      },
      {
        category: 'Civilization Features',
        question: 'Which feature included large projects like walls and canals that helped the community?',
        choices: ['Public works', 'Social classes', 'Job specialization', 'Maritime trade'],
        answer: 'Public works',
        explanation: 'Public works were costly community projects ordered by the government to protect or support people.',
      },
      {
        category: 'Civilization Features',
        question: 'How did social classes develop in early civilizations?',
        choices: ['Specialized jobs created a social structure with different levels of status', 'Everyone became equal because cities grew larger', 'Trade disappeared and people stopped working', 'Religious leaders banned all social rankings'],
        answer: 'Specialized jobs created a social structure with different levels of status',
        explanation: 'The notes connect specialized jobs, educated leaders, warriors, kings, and slaves to social hierarchy.',
      },
      {
        category: 'Mesopotamia',
        question: 'Where was Mesopotamia located?',
        choices: ['In the Fertile Crescent between the Tigris and Euphrates rivers', 'Along the Nile River in northeast Africa', 'On the seven hills by the Tiber River', 'On a wedge-shaped subcontinent below the Himalayas'],
        answer: 'In the Fertile Crescent between the Tigris and Euphrates rivers',
        explanation: 'Mesopotamia means land between two rivers, especially the Tigris and Euphrates.',
      },
      {
        category: 'Mesopotamia',
        question: 'How did many Mesopotamian city-states operate politically and religiously?',
        choices: ['As theocracies with ziggurat temples at the center', 'As democracies where all people voted equally', 'As republics led by two elected consuls', 'As nomadic groups without temples or cities'],
        answer: 'As theocracies with ziggurat temples at the center',
        explanation: 'The notes say city-states operated as theocracies and had ziggurats at their centers.',
      },
      {
        category: 'Mesopotamia',
        question: 'What does polytheistic mean?',
        choices: ['Believing in many gods', 'Believing in only one god', 'Having no religion at all', 'Worshiping only a king as a god'],
        answer: 'Believing in many gods',
        explanation: 'The Sumerians had polytheistic beliefs, meaning they believed in many gods.',
      },
      {
        category: 'Mesopotamia',
        question: 'Which group correctly shows Mesopotamian inventions or achievements?',
        choices: ['Cuneiform writing, the wheel, mud-brick construction, arches, and domes', 'Pyramids, mummification, and the Book of the Dead', 'Alphabetic writing, purple dye, and transparent glassmaking', 'Aqueducts, Latin, and elected consuls'],
        answer: 'Cuneiform writing, the wheel, mud-brick construction, arches, and domes',
        explanation: 'Those inventions and building ideas are listed in the Mesopotamia notes.',
      },
      {
        category: 'Mesopotamia',
        question: 'What was the Epic of Gilgamesh?',
        choices: ['A Mesopotamian cultural work connected to their legacy', 'A Roman law code used by consuls', 'An Egyptian guide for mummification', 'A Phoenician trading route across the sea'],
        answer: 'A Mesopotamian cultural work connected to their legacy',
        explanation: 'The notes list the Epic of Gilgamesh as part of Mesopotamia\'s intellectual and cultural legacy.',
      },
      {
        category: 'Hammurabi',
        question: 'Why was the Code of Hammurabi important?',
        choices: ['It was a written law system that made rules public and helped keep order', 'It ended all social classes in Babylon', 'It made Babylon stop trading with other cities', 'It required every civilization to use the same alphabet'],
        answer: 'It was a written law system that made rules public and helped keep order',
        explanation: 'The code put laws in writing so people could know their rights and responsibilities.',
      },
      {
        category: 'Hammurabi',
        question: 'What idea is the Code of Hammurabi famous for?',
        choices: ['An eye for an eye, meaning punishment should fit the crime', 'Trial by jury for free male citizens', 'A single road system with 111 relay stations', 'Mummification to preserve the body for the afterlife'],
        answer: 'An eye for an eye, meaning punishment should fit the crime',
        explanation: 'The notes say the code is famous for the idea that punishment should fit the crime.',
      },
      {
        category: 'Hammurabi',
        question: 'How were punishments handled under Hammurabi\'s Code?',
        choices: ['They often depended on a person\'s social class or status', 'They were always exactly equal for every person', 'They were chosen only by religious leaders', 'They were never written down'],
        answer: 'They often depended on a person\'s social class or status',
        explanation: 'The notes say not everyone was treated equally because punishments depended on status.',
      },
      {
        category: 'Egypt',
        question: 'Which river was ancient Egypt centered around?',
        choices: ['The Nile River', 'The Tigris River', 'The Euphrates River', 'The Tiber River'],
        answer: 'The Nile River',
        explanation: 'The Egypt notes say the civilization was centered along the Nile River.',
      },
      {
        category: 'Egypt',
        question: 'Which statement about Upper and Lower Egypt is correct?',
        choices: ['Lower Egypt was in the north, and Upper Egypt was in the south', 'Lower Egypt was in the south, and Upper Egypt was in the north', 'Both were in Mesopotamia', 'Both were located in Greece'],
        answer: 'Lower Egypt was in the north, and Upper Egypt was in the south',
        explanation: 'The notes specifically list Lower Egypt in the north and Upper Egypt in the south.',
      },
      {
        category: 'Egypt',
        question: 'How did Egyptians view the pharaoh?',
        choices: ['As both a supreme monarch and a god', 'As a normal merchant chosen by vote', 'As a tribal leader with no religious power', 'As a foreign ruler from Greece'],
        answer: 'As both a supreme monarch and a god',
        explanation: 'The notes say Egyptians revered the ruling pharaoh as both a supreme monarch and a god.',
      },
      {
        category: 'Egypt',
        question: 'What did Egyptian afterlife beliefs include?',
        choices: ['Eternal life after death, the Book of the Dead, and mummification', 'Ziggurats, city-state theocracies, and cuneiform', 'Trial by jury, naval power, and democracy', 'The Royal Road, relay stations, and metal coinage'],
        answer: 'Eternal life after death, the Book of the Dead, and mummification',
        explanation: 'Those were part of Egyptian afterlife practices.',
      },
      {
        category: 'Egypt',
        question: 'How was Egypt connected to nearby regions?',
        choices: ['Through overland routes, Mediterranean sea routes, and Nile River routes', 'Only by crossing the Alps with war elephants', 'Only through isolated mountain passes near the Himalayas', 'Through one road with 111 relay stations'],
        answer: 'Through overland routes, Mediterranean sea routes, and Nile River routes',
        explanation: 'The notes connect Egypt to Syria, Palestine, Mesopotamia, Crete, Cyprus, and Nubia by different routes.',
      },
      {
        category: 'Greece',
        question: 'What was Athens known for?',
        choices: ['Democratic government for free males, naval power, and trial by jury', 'Training boys for war at age six and relying on helot labor', 'Building a 1,667-mile royal road across an empire', 'Inventing the first phonetic alphabet'],
        answer: 'Democratic government for free males, naval power, and trial by jury',
        explanation: 'The Greece notes list democracy, naval power, and trial by jury for Athens.',
      },
      {
        category: 'Greece',
        question: 'What was Sparta known for?',
        choices: ['A militaristic society with boys entering training young and helot labor', 'A city-state network of Mediterranean mariners', 'A river civilization ruled by pharaohs', 'A society centered on ziggurat temples'],
        answer: 'A militaristic society with boys entering training young and helot labor',
        explanation: 'The notes describe Sparta as militaristic and dependent on helot labor.',
      },
      {
        category: 'Greece',
        question: 'What was the Peloponnesian War?',
        choices: ['A conflict between Athens and Sparta that shifted dominance to Sparta', 'A war between Rome and Carthage for Mediterranean control', 'A Persian invasion stopped at Marathon only', 'A rebellion that ended Egyptian afterlife practices'],
        answer: 'A conflict between Athens and Sparta that shifted dominance to Sparta',
        explanation: 'The notes say the Peloponnesian War was between the Delian League led by Athens and the Peloponnesian League led by Sparta.',
      },
      {
        category: 'Greece',
        question: 'What happened during the Persian invasions of Greece?',
        choices: ['Darius I was defeated at Marathon, and Xerxes I was later stopped at Salamis and Plataea', 'Hannibal crossed the Alps and captured Athens', 'Cyrus allowed exiled Jews to return to Jerusalem', 'Phoenician traders invented transparent glassmaking'],
        answer: 'Darius I was defeated at Marathon, and Xerxes I was later stopped at Salamis and Plataea',
        explanation: 'The notes list Marathon for Darius I and Salamis and Plataea for Xerxes I.',
      },
      {
        category: 'Greece',
        question: 'Why did Greek city-states eventually fall to Macedonia?',
        choices: ['They were weakened by internal revolts and foreign wars', 'They became too isolated by the Himalayas', 'They stopped using maritime trade routes', 'They were destroyed by the Code of Hammurabi'],
        answer: 'They were weakened by internal revolts and foreign wars',
        explanation: 'The notes say Greece was weakened before being conquered by Alexander the Great.',
      },
      {
        category: 'Phoenicians',
        question: 'What were the Phoenicians especially known for?',
        choices: ['Seafaring, trade, purple dye, glassmaking, and phonetic writing', 'Pyramids, mummification, and pharaohs', 'Helot labor and military training at age six', 'The Royal Road and a vast land empire'],
        answer: 'Seafaring, trade, purple dye, glassmaking, and phonetic writing',
        explanation: 'The Phoenician notes emphasize trade, maritime skill, Tyrian purple dye, glassmaking, and writing.',
      },
      {
        category: 'Phoenicians',
        question: 'Why was Tyrian purple dye valuable?',
        choices: ['It was highly prized and made from sea snails', 'It was mined from iron deposits in Greece', 'It was made from Nile River mud', 'It was used only to write laws on stone'],
        answer: 'It was highly prized and made from sea snails',
        explanation: 'The notes say Phoenicians exported highly prized Tyrian purple dye made from sea snails.',
      },
      {
        category: 'Phoenicians',
        question: 'Why was the Phoenician writing system important?',
        choices: ['It used symbols for distinct spoken sounds and became a foundation for many Western alphabets', 'It was a picture system used only for Egyptian afterlife texts', 'It was a secret code used only by kings', 'It replaced all trade routes in the Mediterranean'],
        answer: 'It used symbols for distinct spoken sounds and became a foundation for many Western alphabets',
        explanation: 'The notes call it the world\'s first phonetic writing system and connect it to modern Western alphabets.',
      },
      {
        category: 'Persia',
        question: 'How did Cyrus the Great help the Persian Empire expand and last?',
        choices: ['He used political tolerance and local autonomy', 'He forced every group to abandon its customs', 'He banned roads and trade across the empire', 'He made every city-state independent from Persia'],
        answer: 'He used political tolerance and local autonomy',
        explanation: 'The notes say Cyrus expanded using tolerance and local autonomy.',
      },
      {
        category: 'Persia',
        question: 'What was one example of Cyrus the Great\'s benevolent rule?',
        choices: ['He allowed exiled Jews to return to Jerusalem', 'He ordered Rome to destroy Carthage', 'He forced Athenians to join Sparta', 'He ended all trade with Phoenicia'],
        answer: 'He allowed exiled Jews to return to Jerusalem',
        explanation: 'The notes specifically mention Cyrus allowing exiled Jews to return.',
      },
      {
        category: 'Persia',
        question: 'What was the Royal Road?',
        choices: ['A 1,667-mile road network with relay stations for fast communication', 'A canal built by Egypt to connect to Nubia', 'A Greek path from Athens to Sparta', 'A Roman bridge over the Tiber River'],
        answer: 'A 1,667-mile road network with relay stations for fast communication',
        explanation: 'The notes say the Royal Road had 111 relay stations and let royal messengers cross the empire quickly.',
      },
      {
        category: 'Persia',
        question: 'What did Darius I do for the Persian Empire?',
        choices: ['He consolidated imperial control and helped organize the empire', 'He invented the Phoenician alphabet', 'He wrote the Code of Hammurabi', 'He founded Rome on seven hills'],
        answer: 'He consolidated imperial control and helped organize the empire',
        explanation: 'The notes say Darius I consolidated imperial control.',
      },
      {
        category: 'Persia',
        question: 'What religion or teaching spread in Persia from the prophet Zoroaster?',
        choices: ['Teachings about moral struggle and explanations of chaos', 'Egyptian mummification and the Book of the Dead', 'Greek democracy and trial by jury', 'Roman gladiatorial combat'],
        answer: 'Teachings about moral struggle and explanations of chaos',
        explanation: 'The notes connect Zoroaster with philosophical explanations of chaos and moral struggle.',
      },
      {
        category: 'India',
        question: 'How did the Himalayas affect ancient India?',
        choices: ['They helped isolate the subcontinent and contributed to cultural isolation', 'They connected India directly to Rome by sea', 'They made India part of Mesopotamia', 'They prevented any agriculture from developing'],
        answer: 'They helped isolate the subcontinent and contributed to cultural isolation',
        explanation: 'The India notes say the Himalayas isolated the wedge-shaped subcontinent in the north.',
      },
      {
        category: 'India',
        question: 'Why were monsoons important in ancient India?',
        choices: ['Seasonal rains helped agriculture, but too much rain caused floods and too little caused drought', 'They created the Code of Hammurabi', 'They made Greece more democratic', 'They stopped all farming along the Indus River'],
        answer: 'Seasonal rains helped agriculture, but too much rain caused floods and too little caused drought',
        explanation: 'The notes say India relied on seasonal monsoons, with both flooding and drought as risks.',
      },
      {
        category: 'India',
        question: 'Where did the first Indian civilization form?',
        choices: ['Along the Indus River around 2500 BC', 'Along the Nile River around 3100 BCE', 'Between the Tigris and Euphrates rivers around 3700 BCE', 'On the seven hills along the Tiber River'],
        answer: 'Along the Indus River around 2500 BC',
        explanation: 'The notes say the first Indian civilization formed around 2500 BC along the Indus River.',
      },
      {
        category: 'India',
        question: 'Which feature belonged to Indus Valley cities?',
        choices: ['Well-planned grid cities with hilltop fortresses and advanced plumbing', 'Ziggurat temples at the center of city-states', 'Two elected consuls with one-year terms', 'A 1,667-mile road with relay stations'],
        answer: 'Well-planned grid cities with hilltop fortresses and advanced plumbing',
        explanation: 'The notes list planned grid cities, hilltop fortresses, uniform brick homes, and advanced plumbing.',
      },
      {
        category: 'India',
        question: 'What crop or fabric achievement is connected to ancient India in the notes?',
        choices: ['It was the first society to cultivate and weave cotton into fabric', 'It invented Tyrian purple dye from sea snails', 'It created Latin as a written language', 'It first used cuneiform to write laws'],
        answer: 'It was the first society to cultivate and weave cotton into fabric',
        explanation: 'The India notes say the society was the first to cultivate and weave cotton into fabric.',
      },
      {
        category: 'Etruscans and Rome',
        question: 'Where did the Etruscans live?',
        choices: ['Modern Tuscany and Umbria', 'The Fertile Crescent', 'The Indus River valley', 'The Nile River delta'],
        answer: 'Modern Tuscany and Umbria',
        explanation: 'The notes say the Etruscans inhabited modern Tuscany and Umbria.',
      },
      {
        category: 'Etruscans and Rome',
        question: 'How did the Etruscans influence early Rome?',
        choices: ['They influenced Roman architecture, military customs, and gladiatorial combat', 'They invented Egyptian mummification', 'They created the Code of Hammurabi', 'They taught Sparta democratic voting'],
        answer: 'They influenced Roman architecture, military customs, and gladiatorial combat',
        explanation: 'The notes mention the arch, military customs, and gladiatorial combat as Etruscan influences.',
      },
      {
        category: 'Etruscans and Rome',
        question: 'Where was ancient Rome founded?',
        choices: ['On seven hills along the Tiber River in central Italy', 'Between the Tigris and Euphrates rivers', 'Along the Indus River below the Himalayas', 'On Mediterranean city-states controlled by Phoenicia'],
        answer: 'On seven hills along the Tiber River in central Italy',
        explanation: 'The Rome notes say Rome was founded on the seven hills along the Tiber River.',
      },
      {
        category: 'Etruscans and Rome',
        question: 'What happened in 509 BCE in Roman history?',
        choices: ['Romans overthrew Etruscan monarchical rule and formed a representative republic', 'Rome destroyed Carthage after the Punic Wars', 'Hannibal crossed the Alps into Italy', 'Rome became the first Indian civilization'],
        answer: 'Romans overthrew Etruscan monarchical rule and formed a representative republic',
        explanation: 'The notes say Rome overthrew Etruscan rule in 509 BCE to form a republic.',
      },
      {
        category: 'Etruscans and Rome',
        question: 'Who were the patricians in Roman society?',
        choices: ['Wealthy landowning elites', 'Common farmers, merchants, and artisans', 'Enslaved people at the bottom of society', 'Persian royal messengers'],
        answer: 'Wealthy landowning elites',
        explanation: 'The Roman social hierarchy lists patricians as wealthy landowning elites.',
      },
      {
        category: 'Etruscans and Rome',
        question: 'Who were the plebeians in Roman society?',
        choices: ['Common farmers, merchants, and artisans', 'Only the two elected consuls', 'Wealthy landowning elites', 'Foreign kings from Persia'],
        answer: 'Common farmers, merchants, and artisans',
        explanation: 'The notes define plebeians as common farmers, merchants, and artisans.',
      },
      {
        category: 'Etruscans and Rome',
        question: 'What were the Punic Wars fought over?',
        choices: ['Commercial control of the Mediterranean region', 'Control of the Nile River floods', 'The right to use the Phoenician alphabet', 'The return of exiled Jews to Jerusalem'],
        answer: 'Commercial control of the Mediterranean region',
        explanation: 'The notes say Rome fought the Punic Wars for control of Mediterranean commerce.',
      },
      {
        category: 'Etruscans and Rome',
        question: 'What happened after Hannibal crossed the Alps into Italy?',
        choices: ['Rome eventually repelled him, won at Zama, and destroyed Carthage', 'Athens became a stronger democracy', 'Egypt moved south into Nubia', 'Persia adopted the Code of Hammurabi'],
        answer: 'Rome eventually repelled him, won at Zama, and destroyed Carthage',
        explanation: 'The notes say Rome defeated Hannibal and won the Battle of Zama in 202 BCE.',
      },
      {
        category: 'Timeline',
        question: 'Which civilization is matched with the Fertile Crescent and the phrase "land between two rivers"?',
        choices: ['Mesopotamia', 'Ancient Egypt', 'Ancient India', 'Ancient Rome'],
        answer: 'Mesopotamia',
        explanation: 'Mesopotamia was located between the Tigris and Euphrates rivers.',
      },
      {
        category: 'Timeline',
        question: 'Which civilization is matched with the Nile River, pharaohs, and mummification?',
        choices: ['Ancient Egypt', 'Phoenicia', 'Persia', 'Sparta'],
        answer: 'Ancient Egypt',
        explanation: 'The Nile, pharaohs, the Book of the Dead, and mummification all point to ancient Egypt.',
      },
      {
        category: 'Timeline',
        question: 'Which civilization is most connected with seafaring trade and the foundation of many Western alphabets?',
        choices: ['Phoenicians', 'Etruscans', 'Assyrians', 'Mycenaeans'],
        answer: 'Phoenicians',
        explanation: 'Phoenician trade spread their phonetic writing system through the Mediterranean.',
      },
      {
        category: 'Timeline',
        question: 'Which empire is most connected with Cyrus the Great, Darius I, the Royal Road, and Persepolis?',
        choices: ['Persian Empire', 'Roman Republic', 'Babylonian Empire', 'Indus Valley Civilization'],
        answer: 'Persian Empire',
        explanation: 'Those people and achievements belong to the Persian Empire notes.',
      },
      {
        category: 'Timeline',
        question: 'Which civilization is most connected with planned grid cities, advanced plumbing, cotton, and the Indus River?',
        choices: ['Ancient India', 'Ancient Greece', 'Mesopotamia', 'Phoenicia'],
        answer: 'Ancient India',
        explanation: 'Those details describe the Indus Valley civilization in ancient India.',
      },
    ],
    jeopardyCategories: [
      { name: 'Features', indexes: [0, 1, 2, 3, 4] },
      { name: 'Mesopotamia', indexes: [6, 7, 8, 9, 12] },
      { name: 'Egypt + Greece', indexes: [15, 16, 17, 22, 24] },
      { name: 'Trade + Persia', indexes: [26, 28, 30, 32, 34] },
      { name: 'India + Rome', indexes: [36, 39, 41, 44, 47] },
    ],
  },
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="World history study guide with interactive review games." />
    <meta name="generator" content={Astro.generator} />
    <title>World History Study Guide</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@500;700&family=Cormorant+Garamond:wght@400;500;600;700&family=Luckiest+Guy&family=Nunito:wght@500;600;700;800;900&display=swap" rel="stylesheet" />
    <style>
      :root {
        --sand: #f4ebda;
        --sand-dark: #ddc6a2;
        --ink: #382116;
        --rust: #7a2f24;
        --rust-deep: #541b16;
        --gold: #ba8a4b;
        --card: rgba(250, 245, 235, 0.95);
        --line: rgba(84, 27, 22, 0.14);
        --shadow: 0 24px 55px rgba(52, 32, 21, 0.15);
        --success: #1d6a45;
        --danger: #8d3027;
      }

      * { box-sizing: border-box; }

      body {
        margin: 0;
        min-height: 100vh;
        color: var(--ink);
        font-family: 'Cormorant Garamond', Georgia, serif;
        background:
          radial-gradient(circle at top, rgba(186, 138, 75, 0.18), transparent 30%),
          linear-gradient(180deg, rgba(122, 47, 36, 0.08), transparent 16%),
          linear-gradient(135deg, #f7f0e2 0%, #ead8bc 50%, #dfc8a5 100%);
      }

      body::before {
        content: '';
        position: fixed;
        inset: 0;
        pointer-events: none;
        opacity: 0.18;
        background-image:
          linear-gradient(rgba(84, 27, 22, 0.05) 1px, transparent 1px),
          linear-gradient(90deg, rgba(84, 27, 22, 0.04) 1px, transparent 1px);
        background-size: 28px 28px;
      }

      main {
        width: min(1040px, calc(100% - 1.25rem));
        margin: 0 auto;
        padding: 1.25rem 0 3rem;
      }

      .hero {
        position: relative;
        overflow: hidden;
        padding: 2.2rem;
        border: 1px solid rgba(84, 27, 22, 0.18);
        border-radius: 30px;
        background:
          linear-gradient(145deg, rgba(84, 27, 22, 0.96), rgba(122, 47, 36, 0.9)),
          linear-gradient(180deg, rgba(255, 255, 255, 0.05), transparent);
        color: #f7f0e2;
        box-shadow: var(--shadow);
      }

      .hero::after {
        content: '';
        position: absolute;
        right: -3rem;
        bottom: -5rem;
        width: 15rem;
        height: 15rem;
        border-radius: 50%;
        background: radial-gradient(circle, rgba(186, 138, 75, 0.24), transparent 70%);
      }

      h1, h2, h3 {
        margin: 0;
        font-family: 'Cinzel', 'Times New Roman', serif;
      }

      h1 {
        margin-top: 1rem;
        font-size: clamp(2.4rem, 5vw, 4.3rem);
        line-height: 0.95;
      }

      .hero-copy {
        max-width: 40rem;
        margin-top: 1rem;
        font-size: clamp(1.15rem, 2vw, 1.42rem);
        line-height: 1.5;
        color: rgba(247, 240, 226, 0.92);
      }

      .menu-wrap {
        margin-top: 1.3rem;
      }

      .hero-top {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        gap: 1rem;
      }

      .unit-switcher {
        display: flex;
        flex-wrap: wrap;
        gap: 0.55rem;
      }

      .unit-chip {
        padding: 0.55rem 0.95rem;
        border: 1px solid rgba(255, 255, 255, 0.12);
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.08);
        color: #fff8ec;
        cursor: pointer;
        transition: transform 0.2s ease, background 0.2s ease, border-color 0.2s ease;
        font-family: 'Cinzel', 'Times New Roman', serif;
        letter-spacing: 0.04em;
        font-size: 0.92rem;
        font-weight: 700;
      }

      .unit-chip:hover {
        transform: translateY(-2px);
        background: rgba(255, 255, 255, 0.12);
        border-color: rgba(255, 255, 255, 0.18);
      }

      .unit-chip.active {
        background: linear-gradient(135deg, rgba(186, 138, 75, 0.28), rgba(84, 27, 22, 0.18));
        border-color: rgba(255, 255, 255, 0.24);
        box-shadow: inset 0 0 0 1px rgba(255, 248, 236, 0.1);
      }

      .menu-label {
        margin: 0 0 0.75rem;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        font-size: 0.95rem;
        color: rgba(247, 240, 226, 0.82);
      }

      .mode-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 1rem;
      }

      .mode-card {
        width: 100%;
        min-height: 150px;
        padding: 1.2rem;
        border: 1px solid rgba(255, 255, 255, 0.12);
        border-radius: 24px;
        background: rgba(255, 255, 255, 0.08);
        color: #fff8ec;
        text-align: left;
        cursor: pointer;
        transition: transform 0.2s ease, background 0.2s ease, border-color 0.2s ease;
      }

      .mode-card:hover {
        transform: translateY(-2px);
        background: rgba(255, 255, 255, 0.12);
        border-color: rgba(255, 255, 255, 0.18);
      }

      .mode-card.active {
        background: linear-gradient(135deg, rgba(186, 138, 75, 0.28), rgba(84, 27, 22, 0.18));
        border-color: rgba(255, 255, 255, 0.24);
        box-shadow: inset 0 0 0 1px rgba(255, 248, 236, 0.1);
      }

      .mode-card strong {
        display: block;
        font-family: 'Cinzel', 'Times New Roman', serif;
        font-size: 1.28rem;
      }

      .mode-card span {
        display: block;
        margin-top: 0.6rem;
        font-size: 1.05rem;
        line-height: 1.4;
        color: rgba(255, 248, 236, 0.88);
      }

      .stats-launch {
        flex-shrink: 0;
        padding: 0.6rem 0.95rem;
        border: 1px solid rgba(255, 255, 255, 0.16);
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.1);
        color: #fff8ec;
        font-family: 'Nunito', 'Trebuchet MS', sans-serif;
        font-size: 0.88rem;
        font-weight: 800;
        letter-spacing: 0.04em;
        text-transform: uppercase;
        cursor: pointer;
        transition: transform 0.2s ease, background 0.2s ease, border-color 0.2s ease;
      }

      .stats-launch:hover,
      .stats-launch.active {
        transform: translateY(-2px);
        background: rgba(255, 255, 255, 0.16);
        border-color: rgba(255, 255, 255, 0.24);
      }

      .board {
        margin-top: 1.4rem;
        padding: 1.2rem;
        border-radius: 28px;
        background: rgba(255, 252, 246, 0.58);
        border: 1px solid rgba(84, 27, 22, 0.1);
        box-shadow: 0 18px 42px rgba(52, 32, 21, 0.08);
        backdrop-filter: blur(10px);
      }

      .board[hidden] {
        display: none;
      }

      .panel {
        padding: 1.4rem;
        border-radius: 24px;
        background: var(--card);
        border: 1px solid var(--line);
      }

      :global(.panel-top) {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 1rem;
        margin-bottom: 1rem;
        flex-wrap: wrap;
      }

      :global(.panel-top p) {
        margin: 0.25rem 0 0;
        font-size: 1rem;
        font-weight: 700;
        color: #5f4d41;
      }

      :global(.panel-top h2),
      :global(.result h2) {
        font-family: 'Cinzel', 'Times New Roman', serif;
        font-size: 2rem;
        font-weight: 700;
        letter-spacing: 0.04em;
        line-height: 1.05;
        color: var(--rust-deep);
      }

      :global(.pill) {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0.55rem 0.9rem;
        border-radius: 999px;
        background: linear-gradient(135deg, var(--rust), var(--rust-deep));
        color: #fff8ec;
        font-size: 0.82rem;
        font-weight: 700;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        box-shadow: 0 4px 14px rgba(154,74,54,0.22);
      }

      :global(.progress-track) {
        width: 100%;
        height: 12px;
        border-radius: 999px;
        background: rgba(122, 47, 36, 0.12);
        overflow: hidden;
      }

      :global(.progress-fill) {
        height: 100%;
        border-radius: 999px;
        background: linear-gradient(90deg, var(--gold), #d7a462);
        transition: width 0.25s ease;
      }

      :global(.question) {
        margin-top: 1rem;
        font-size: 1.28rem;
        line-height: 1.75;
        font-weight: 700;
        color: #2f241e;
        font-family: 'Nunito', 'Trebuchet MS', sans-serif;
        letter-spacing: 0.01em;
      }

      :global(.choices) {
        display: grid;
        gap: 0.85rem;
        margin-top: 1.2rem;
      }

      :global(.choice) {
        width: 100%;
        padding: 1rem 1.05rem;
        border-radius: 16px;
        border: 2px solid transparent;
        background: rgba(255, 255, 255, 0.97);
        color: #2f241e;
        font-size: 1.08rem;
        font-weight: 800;
        line-height: 1.5;
        font-family: 'Nunito', 'Trebuchet MS', sans-serif;
        letter-spacing: 0.01em;
        text-align: left;
        cursor: pointer;
        box-shadow: 0 3px 12px rgba(0,0,0,0.06);
        transition: all 0.2s ease;
      }

      :global(.choice:hover:enabled) { transform: translateY(-2px); filter: brightness(1.02); }
      :global(.choice.correct) { background: rgba(29, 106, 69, 0.12); border-color: rgba(29, 106, 69, 0.38); color: var(--success); }
      :global(.choice.wrong) { background: rgba(141, 48, 39, 0.12); border-color: rgba(141, 48, 39, 0.35); color: var(--danger); }

      :global(.feedback) {
        margin-top: 1rem;
        padding: 1rem;
        border-left: 4px solid var(--gold);
        border-radius: 16px;
        background: rgba(186, 138, 75, 0.12);
      }

      :global(.feedback strong) {
        display: block;
        margin-bottom: 0.35rem;
        color: #4b2f24;
        font-size: 1.02rem;
        font-family: 'Nunito', 'Trebuchet MS', sans-serif;
        font-weight: 800;
      }

      :global(.feedback p) {
        margin: 0;
        font-size: 1.1rem;
        line-height: 1.55;
        color: #3c2b21;
        font-family: 'Nunito', 'Trebuchet MS', sans-serif;
        font-weight: 700;
      }

      :global(.actions) {
        display: flex;
        gap: 0.85rem;
        margin-top: 1.1rem;
        flex-wrap: wrap;
      }

      :global(.btn) {
        border: none;
        border-radius: 999px;
        padding: 0.9rem 1.3rem;
        font-size: 0.95rem;
        font-weight: 800;
        font-family: 'Nunito', 'Trebuchet MS', sans-serif;
        letter-spacing: 0.01em;
        cursor: pointer;
        transition: all 0.2s ease;
      }

      :global(.btn-primary) { background: linear-gradient(135deg, var(--rust), var(--rust-deep)); color: white; box-shadow: 0 4px 14px rgba(122,47,36,0.24); }
      :global(.btn-secondary) { background: rgba(243,244,246,0.85); color: #5f4d41; }
      :global(.btn:hover) { transform: translateY(-2px); }

      :global(.typing-input) {
        width: 100%;
        margin-top: 1.1rem;
        padding: 1rem;
        border-radius: 18px;
        border: 1px solid rgba(84, 27, 22, 0.16);
        background: rgba(255,255,255,0.92);
        color: #374151;
        font-size: 1rem;
        font-family: 'Nunito', 'Trebuchet MS', sans-serif;
        font-weight: 700;
      }

      :global(.typing-help) {
        margin-top: 0.7rem;
        font-size: 1rem;
        color: #5f4d41;
        font-weight: 700;
        font-family: 'Nunito', 'Trebuchet MS', sans-serif;
      }

      :global(.ladder) {
        display: grid;
        gap: 0.45rem;
        margin-top: 1.2rem;
      }

      :global(.ladder-step) {
        padding: 0.75rem 0.9rem;
        border-radius: 14px;
        background: rgba(255,255,255,0.72);
        color: #6f7280;
        box-shadow: 0 2px 8px rgba(0,0,0,0.04);
      }

      :global(.ladder-step.active) {
        background: linear-gradient(135deg, var(--rust), var(--rust-deep));
        color: #fff8ec;
      }

      :global(.jeopardy-grid) {
        display: grid;
        grid-template-columns: repeat(5, minmax(0, 1fr));
        gap: 0.6rem;
        margin-top: 1rem;
      }

      :global(.jeopardy-head),
      :global(.jeopardy-cell) {
        padding: 0.9rem 0.55rem;
        border-radius: 16px;
        text-align: center;
      }

      :global(.jeopardy-head) {
        background: linear-gradient(135deg, var(--rust), var(--rust-deep));
        color: #fff8ec;
        font-family: 'Cinzel', 'Times New Roman', serif;
        font-size: 0.82rem;
        font-weight: 800;
      }

      :global(.jeopardy-cell) {
        border: none;
        background: linear-gradient(135deg, #9f4733, #7a2f24);
        color: white;
        font-weight: 700;
        cursor: pointer;
        box-shadow: 0 4px 14px rgba(159,71,51,0.22);
      }

      :global(.jeopardy-cell.done) {
        background: rgba(255,255,255,0.82);
        color: #8f7669;
        cursor: default;
        box-shadow: none;
      }

      :global(.result) {
        text-align: center;
      }

      :global(.result p) {
        font-size: 1.14rem;
        line-height: 1.45;
      }

      :global(.stats-grid) {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 0.9rem;
      }

      :global(.stats-card) {
        padding: 1rem 1.05rem;
        border-radius: 18px;
        background: rgba(255, 255, 255, 0.9);
        border: 1px solid rgba(84, 27, 22, 0.12);
        box-shadow: 0 8px 24px rgba(52, 32, 21, 0.08);
      }

      :global(.stats-card strong) {
        display: block;
        color: var(--rust-deep);
        font-family: 'Cinzel', 'Times New Roman', serif;
        font-size: 1rem;
      }

      :global(.stats-card p) {
        margin: 0.35rem 0 0;
        color: #5f4d41;
        font-family: 'Nunito', 'Trebuchet MS', sans-serif;
        font-size: 0.98rem;
        font-weight: 700;
      }

      :global(.stats-percent) {
        margin-top: 0.5rem;
        color: #2f241e;
        font-family: 'Nunito', 'Trebuchet MS', sans-serif;
        font-size: 1.9rem;
        font-weight: 900;
      }

      :global(.stats-missed) {
        margin-top: 1.1rem;
        padding-top: 1.1rem;
        border-top: 1px solid rgba(84, 27, 22, 0.12);
      }

      :global(.missed-list) {
        display: grid;
        gap: 0.8rem;
        margin-top: 1rem;
      }

      :global(.missed-item) {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 0.9rem;
        padding: 0.95rem 1rem;
        border-radius: 18px;
        background: rgba(255, 255, 255, 0.9);
        border: 1px solid rgba(84, 27, 22, 0.12);
      }

      :global(.missed-tag) {
        display: inline-flex;
        align-items: center;
        padding: 0.28rem 0.6rem;
        border-radius: 999px;
        background: rgba(122, 47, 36, 0.1);
        color: var(--rust-deep);
        font-family: 'Nunito', 'Trebuchet MS', sans-serif;
        font-size: 0.8rem;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 0.03em;
      }

      :global(.missed-question) {
        margin-top: 0.5rem;
        color: #2f241e;
        font-family: 'Nunito', 'Trebuchet MS', sans-serif;
        font-size: 1rem;
        font-weight: 800;
        line-height: 1.45;
      }

      @media (max-width: 760px) {
        main { width: min(100% - 0.75rem, 42rem); padding-top: 0.8rem; }
        .hero { padding: 1.5rem; border-radius: 22px; }
        .hero-top { flex-direction: column; align-items: stretch; }
        .stats-launch { align-self: flex-end; }
        .mode-grid { grid-template-columns: 1fr; }
        .board { padding: 0.9rem; border-radius: 22px; }
        .panel { padding: 1rem; }
        :global(.jeopardy-grid) { grid-template-columns: repeat(2, minmax(0, 1fr)); }
        :global(.jeopardy-head) { display: none; }
        :global(.stats-grid) { grid-template-columns: 1fr; }
        :global(.missed-item) { align-items: flex-start; flex-direction: column; }
      }
    </style>
  </head>
  <body>
    <main>
      <section class="hero">
        <div class="unit-switcher">
          <button class="unit-chip active" data-unit="unit-1" type="button">Unit One • First Peoples + Societies</button>
          <button class="unit-chip" data-unit="unit-2" type="button">Unit Two • Ancient World Civilizations</button>
        </div>
        <div class="hero-top">
          <div>
            <h1>World History Study Guide</h1>
            <p class="hero-copy">Choose your unit and study style and start studying!</p>
          </div>
          <button class="stats-launch" data-mode="stats" type="button">Stats</button>
        </div>
        <div class="menu-wrap">
          <p class="menu-label">Choose a mode</p>
          <div class="mode-grid">
            <button class="mode-card" data-mode="multiple" type="button">
              <strong>Multiple Choice</strong>
              <span>Classic one-question practice with instant feedback.</span>
            </button>
            <button class="mode-card" data-mode="jeopardy" type="button">
              <strong>Jeopardy</strong>
              <span>Pick dollar values from a board and clear the whole set.</span>
            </button>
            <button class="mode-card" data-mode="millionaire" type="button">
              <strong>Millionaire</strong>
              <span>Climb the prize ladder one history question at a time.</span>
            </button>
            <button class="mode-card" data-mode="typing" type="button">
              <strong>Typing Quiz</strong>
              <span>Type the answer yourself instead of picking from choices.</span>
            </button>
          </div>
        </div>
      </section>

      <section class="board" id="game-board" hidden>
        <div id="game-root" class="panel"></div>
      </section>
    </main>

    <script type="application/json" id="history-units" set:html={JSON.stringify(studyUnits)}></script>
    <script>
      const units = JSON.parse(document.getElementById('history-units').textContent || '[]');
      const root = document.getElementById('game-root');
      const board = document.getElementById('game-board');
      const modeButtons = Array.from(document.querySelectorAll('[data-mode]'));
      const unitButtons = Array.from(document.querySelectorAll('[data-unit]'));
      const prizeLadder = ['$100', '$200', '$300', '$500', '$1,000', '$2,000', '$4,000', '$8,000', '$16,000', '$32,000'];
      const statsStorageKey = 'history-stats-v1';
      let currentUnit = units[0];

      let state = {};
      let stats = loadStats();

      function shuffle(items) {
        const copy = [...items];
        for (let i = copy.length - 1; i > 0; i -= 1) {
          const j = Math.floor(Math.random() * (i + 1));
          [copy[i], copy[j]] = [copy[j], copy[i]];
        }
        return copy;
      }

      function normalize(value) {
        return value.toLowerCase().replace(/[^\w\s]/g, ' ').replace(/\s+/g, ' ').trim();
      }

      function keywordSet(value) {
        const skipWords = new Set(['a', 'an', 'and', 'are', 'as', 'at', 'be', 'by', 'for', 'from', 'in', 'into', 'is', 'it', 'of', 'on', 'or', 'that', 'the', 'their', 'this', 'to', 'was', 'were', 'with']);
        return new Set(
          normalize(value)
            .split(' ')
            .filter((word) => word.length > 2 && !skipWords.has(word))
        );
      }

      function isLooseMatch(guess, answer, explanation = '') {
        if (!guess) return false;
        const combined = normalize(`${answer} ${explanation}`);
        if (guess === answer || guess.includes(answer) || answer.includes(guess)) return true;
        if (guess.includes(combined) || combined.includes(guess)) return true;

        const guessWords = keywordSet(guess);
        const answerWords = keywordSet(combined);
        if (!guessWords.size || !answerWords.size) return false;

        let overlap = 0;
        guessWords.forEach((word) => {
          if (answerWords.has(word)) overlap += 1;
        });

        const overlapRatio = overlap / answerWords.size;
        return overlap >= 2 || overlapRatio >= 0.5;
      }

      function currentQuestions() {
        return currentUnit.questions;
      }

      function currentJeopardyCategories() {
        return currentUnit.jeopardyCategories;
      }

      function questionKey(item) {
        return `${currentUnit.id}::${item.category}::${item.question}`;
      }

      function createEmptyUnitStats(unit) {
        const categories = [...new Set(unit.questions.map((item) => item.category))];
        return {
          categories: Object.fromEntries(categories.map((category) => [category, { correct: 0, attempts: 0 }])),
          missed: {},
        };
      }

      function ensureUnitStats(unitId) {
        if (!stats[unitId]) {
          const unit = units.find((entry) => entry.id === unitId) || units[0];
          stats[unitId] = createEmptyUnitStats(unit);
        }

        const unit = units.find((entry) => entry.id === unitId) || units[0];
        unit.questions.forEach((item) => {
          if (!stats[unitId].categories[item.category]) {
            stats[unitId].categories[item.category] = { correct: 0, attempts: 0 };
          }
        });

        return stats[unitId];
      }

      function loadStats() {
        try {
          const saved = JSON.parse(localStorage.getItem(statsStorageKey) || '{}');
          if (saved && typeof saved === 'object') return saved;
        } catch {}
        return {};
      }

      function saveStats() {
        localStorage.setItem(statsStorageKey, JSON.stringify(stats));
      }

      function confirmStatsReset() {
        const typed = window.prompt('Are you sure you want to do this? Type PAST to reset your stats.');
        return typed === 'PAST';
      }

      function recordAnswer(item, correct) {
        const unitStats = ensureUnitStats(currentUnit.id);
        const categoryStats = unitStats.categories[item.category] || { correct: 0, attempts: 0 };
        categoryStats.attempts += 1;
        if (correct) categoryStats.correct += 1;
        unitStats.categories[item.category] = categoryStats;

        const key = questionKey(item);
        if (correct) {
          delete unitStats.missed[key];
        } else {
          unitStats.missed[key] = {
            category: item.category,
            question: item.question,
            answer: item.answer,
            explanation: item.explanation,
            choices: item.choices,
          };
        }

        saveStats();
      }

      function accuracyPercent(correct, attempts) {
        if (!attempts) return '0%';
        return `${Math.round((correct / attempts) * 100)}%`;
      }

      function currentUnitStats() {
        return ensureUnitStats(currentUnit.id);
      }

      function missedQuestions() {
        return Object.values(currentUnitStats().missed);
      }

      function buildMissedQuestions() {
        return missedQuestions().map((item) => ({ ...item, choices: shuffle(item.choices) }));
      }

      function buildMcQuestions() {
        return shuffle(currentQuestions()).map((item) => ({ ...item, choices: shuffle(item.choices) }));
      }

      function setActiveUnit(unitId) {
        currentUnit = units.find((unit) => unit.id === unitId) || units[0];
        unitButtons.forEach((button) => {
          button.classList.toggle('active', button.dataset.unit === currentUnit.id);
        });
      }

      function setActiveMode(mode) {
        board.hidden = false;
        modeButtons.forEach((button) => {
          button.classList.toggle('active', button.dataset.mode === mode);
        });
      }

      function initMultiple() {
        state = { mode: 'multiple', deck: buildMcQuestions(), index: 0, score: 0, locked: false };
        setActiveMode('multiple');
        renderMultiple();
      }

      function renderMultiple() {
        const item = state.deck[state.index];
        const progress = ((state.index + 1) / state.deck.length) * 100;
        root.innerHTML = `
          <div class="panel-top">
            <div>
              <h2>Multiple Choice</h2>
              <p>Question ${state.index + 1} of ${state.deck.length}</p>
            </div>
            <div class="pill">Score ${state.score}</div>
          </div>
          <div class="progress-track"><div class="progress-fill" style="width:${progress}%"></div></div>
          <div class="question">${item.question}</div>
          <div class="choices">
            ${item.choices.map((choice) => `<button class="choice" data-choice="${escapeHtml(choice)}">${choice}</button>`).join('')}
          </div>
          <div class="feedback" id="feedback" hidden>
            <strong id="feedback-title"></strong>
            <p id="feedback-copy"></p>
          </div>
          <div class="actions">
            <button class="btn btn-primary" id="next-btn" hidden>${state.index === state.deck.length - 1 ? 'See Score' : 'Next Question'}</button>
            <button class="btn btn-secondary" id="restart-btn">Start Over</button>
          </div>
        `;

        root.querySelectorAll('[data-choice]').forEach((button) => {
          button.addEventListener('click', () => handleMultipleChoice(button, button.textContent, item));
        });
        root.querySelector('#restart-btn').addEventListener('click', initMultiple);
      }

      function handleMultipleChoice(button, choice, item) {
        if (state.locked) return;
        state.locked = true;
        const feedback = root.querySelector('#feedback');
        const feedbackTitle = root.querySelector('#feedback-title');
        const feedbackCopy = root.querySelector('#feedback-copy');
        const nextBtn = root.querySelector('#next-btn');
        root.querySelectorAll('[data-choice]').forEach((entry) => {
          entry.disabled = true;
          if (entry.textContent === item.answer) entry.classList.add('correct');
        });
        const correct = choice === item.answer;
        recordAnswer(item, correct);
        if (correct) {
          state.score += 1;
          button.classList.add('correct');
          feedbackTitle.textContent = 'Correct';
        } else {
          button.classList.add('wrong');
          feedbackTitle.textContent = `Not quite. The answer is ${item.answer}.`;
        }
        feedbackCopy.textContent = item.explanation;
        feedback.hidden = false;
        nextBtn.hidden = false;
        nextBtn.addEventListener('click', () => {
          if (state.index === state.deck.length - 1) {
            renderResult('Multiple Choice', state.score, state.deck.length, initMultiple);
            return;
          }
          state.index += 1;
          state.locked = false;
          renderMultiple();
        }, { once: true });
      }

      function initTyping() {
        state = { mode: 'typing', deck: currentQuestions(), index: 0, score: 0 };
        setActiveMode('typing');
        renderTyping();
      }

      function renderTyping() {
        const item = state.deck[state.index];
        const progress = ((state.index + 1) / state.deck.length) * 100;
        root.innerHTML = `
          <div class="panel-top">
            <div>
              <h2>Typing Quiz</h2>
              <p>Question ${state.index + 1} of ${state.deck.length}</p>
            </div>
            <div class="pill">Score ${state.score}</div>
          </div>
          <div class="progress-track"><div class="progress-fill" style="width:${progress}%"></div></div>
          <div class="question">${item.question}</div>
          <input class="typing-input" id="typing-input" type="text" placeholder="Type your answer here" />
          <div class="typing-help">Short answer is fine. It checks loosely, not word for word.</div>
          <div class="actions">
            <button class="btn btn-primary" id="check-btn">Check Answer</button>
            <button class="btn btn-secondary" id="restart-btn">Start Over</button>
          </div>
          <div class="feedback" id="feedback" hidden>
            <strong id="feedback-title"></strong>
            <p id="feedback-copy"></p>
          </div>
        `;

        const input = root.querySelector('#typing-input');
        const checkBtn = root.querySelector('#check-btn');
        const restartBtn = root.querySelector('#restart-btn');
        checkBtn.addEventListener('click', () => handleTyping(item));
        input.addEventListener('keydown', (event) => {
          if (event.key === 'Enter') handleTyping(item);
        });
        restartBtn.addEventListener('click', initTyping);
      }

      function handleTyping(item) {
        const input = root.querySelector('#typing-input');
        const guess = normalize(input.value);
        if (!guess) return;
        const answer = normalize(item.answer);
        const feedback = root.querySelector('#feedback');
        const feedbackTitle = root.querySelector('#feedback-title');
        const feedbackCopy = root.querySelector('#feedback-copy');
        const ok = isLooseMatch(guess, answer, item.explanation);
        recordAnswer(item, ok);
        if (ok) {
          state.score += 1;
          feedbackTitle.textContent = 'Correct';
          feedbackCopy.textContent = item.explanation;
        } else {
          feedbackTitle.textContent = `Not quite. The answer is ${item.answer}.`;
          feedbackCopy.textContent = item.explanation;
        }
        feedback.hidden = false;
        input.disabled = true;
        root.querySelector('#check-btn').hidden = true;
        const next = document.createElement('button');
        next.className = 'btn btn-primary';
        next.textContent = state.index === state.deck.length - 1 ? 'See Score' : 'Next Question';
        next.addEventListener('click', () => {
          if (state.index === state.deck.length - 1) {
            renderResult('Typing Quiz', state.score, state.deck.length, initTyping);
            return;
          }
          state.index += 1;
          renderTyping();
        }, { once: true });
        root.querySelector('.actions').prepend(next);
      }

      function initMillionaire() {
        state = { mode: 'millionaire', deck: shuffle(currentQuestions()).slice(0, Math.min(10, currentQuestions().length)).map((item) => ({ ...item, choices: shuffle(item.choices) })), index: 0, locked: false };
        setActiveMode('millionaire');
        renderMillionaire();
      }

      function renderMillionaire() {
        const item = state.deck[state.index];
        root.innerHTML = `
          <div class="panel-top">
            <div>
              <h2>Millionaire</h2>
              <p>Answer ten in a row and climb the ladder.</p>
            </div>
            <div class="pill">${prizeLadder[state.index]}</div>
          </div>
          <div class="question">${item.question}</div>
          <div class="choices">
            ${item.choices.map((choice) => `<button class="choice" data-choice="${escapeHtml(choice)}">${choice}</button>`).join('')}
          </div>
          <div class="feedback" id="feedback" hidden>
            <strong id="feedback-title"></strong>
            <p id="feedback-copy"></p>
          </div>
          <div class="ladder">
            ${prizeLadder.slice().reverse().map((step, idx) => {
              const actual = prizeLadder.length - 1 - idx;
              return `<div class="ladder-step ${actual === state.index ? 'active' : ''}">${step}</div>`;
            }).join('')}
          </div>
          <div class="actions">
            <button class="btn btn-secondary" id="restart-btn">Start Over</button>
          </div>
        `;

        root.querySelectorAll('[data-choice]').forEach((button) => {
          button.addEventListener('click', () => handleMillionaireChoice(button, button.textContent, item));
        });
        root.querySelector('#restart-btn').addEventListener('click', initMillionaire);
      }

      function handleMillionaireChoice(button, choice, item) {
        if (state.locked) return;
        state.locked = true;
        const feedback = root.querySelector('#feedback');
        const feedbackTitle = root.querySelector('#feedback-title');
        const feedbackCopy = root.querySelector('#feedback-copy');
        root.querySelectorAll('[data-choice]').forEach((entry) => {
          entry.disabled = true;
          if (entry.textContent === item.answer) entry.classList.add('correct');
        });
        const correct = choice === item.answer;
        recordAnswer(item, correct);
        if (correct) {
          button.classList.add('correct');
          feedbackTitle.textContent = `Correct. You reached ${prizeLadder[state.index]}.`;
          feedbackCopy.textContent = item.explanation;
          const next = document.createElement('button');
          next.className = 'btn btn-primary';
          next.textContent = state.index === state.deck.length - 1 ? 'Claim Win' : 'Next Question';
          next.addEventListener('click', () => {
            if (state.index === state.deck.length - 1) {
              renderResult('Millionaire', state.deck.length, state.deck.length, initMillionaire, 'You cleared the whole ladder.');
              return;
            }
            state.index += 1;
            state.locked = false;
            renderMillionaire();
          }, { once: true });
          root.querySelector('.actions').prepend(next);
        } else {
          button.classList.add('wrong');
          feedbackTitle.textContent = `Game over. The answer is ${item.answer}.`;
          feedbackCopy.textContent = item.explanation;
          const retry = document.createElement('button');
          retry.className = 'btn btn-primary';
          retry.textContent = 'Play Again';
          retry.addEventListener('click', initMillionaire, { once: true });
          root.querySelector('.actions').prepend(retry);
        }
        feedback.hidden = false;
      }

      function initJeopardy() {
        state = {
          mode: 'jeopardy',
          answered: {},
          activeCell: null,
          score: 0,
        };
        setActiveMode('jeopardy');
        renderJeopardyBoard();
      }

      function renderJeopardyBoard() {
        const jeopardyCategories = currentJeopardyCategories();
        const rowCount = Math.max(...jeopardyCategories.map((category) => category.indexes.length));
        const remaining = currentQuestions().length - Object.keys(state.answered).length;
        root.innerHTML = `
          <div class="panel-top">
            <div>
              <h2>Jeopardy</h2>
              <p>Pick a square and clear the board.</p>
            </div>
            <div class="pill">Score ${state.score}</div>
          </div>
          <div class="jeopardy-grid">
            ${jeopardyCategories.map((category) => `<div class="jeopardy-head">${category.name}</div>`).join('')}
            ${Array.from({ length: rowCount }, (_, row) => jeopardyCategories.map((category) => {
              const questionIndex = category.indexes[row];
              if (questionIndex === undefined) return '<div></div>';
              const key = String(questionIndex);
              const done = state.answered[key];
              return `<button class="jeopardy-cell ${done ? 'done' : ''}" data-j-index="${questionIndex}" ${done ? 'disabled' : ''}>$${(row + 1) * 100}</button>`;
            }).join('')).join('')}
          </div>
          <div class="typing-help">${remaining} square${remaining === 1 ? '' : 's'} left.</div>
        `;

        root.querySelectorAll('[data-j-index]').forEach((button) => {
          button.addEventListener('click', () => openJeopardyQuestion(Number(button.dataset.jIndex)));
        });
      }

      function openJeopardyQuestion(index) {
        const item = { ...currentQuestions()[index], choices: shuffle(currentQuestions()[index].choices) };
        state.activeCell = index;
        root.innerHTML = `
          <div class="panel-top">
            <div>
              <h2>Jeopardy</h2>
              <p>Choose the best answer.</p>
            </div>
            <div class="pill">$${(findJeopardyRow(index) + 1) * 100}</div>
          </div>
          <div class="question">${item.question}</div>
          <div class="choices">
            ${item.choices.map((choice) => `<button class="choice" data-choice="${escapeHtml(choice)}">${choice}</button>`).join('')}
          </div>
          <div class="feedback" id="feedback" hidden>
            <strong id="feedback-title"></strong>
            <p id="feedback-copy"></p>
          </div>
          <div class="actions">
            <button class="btn btn-secondary" id="board-btn">Back to Board</button>
          </div>
        `;

        root.querySelectorAll('[data-choice]').forEach((button) => {
          button.addEventListener('click', () => handleJeopardyChoice(button, button.textContent, item));
        });
        root.querySelector('#board-btn').addEventListener('click', renderJeopardyBoard);
      }

      function handleJeopardyChoice(button, choice, item) {
        const feedback = root.querySelector('#feedback');
        const feedbackTitle = root.querySelector('#feedback-title');
        const feedbackCopy = root.querySelector('#feedback-copy');
        const value = (findJeopardyRow(state.activeCell) + 1) * 100;
        root.querySelectorAll('[data-choice]').forEach((entry) => {
          entry.disabled = true;
          if (entry.textContent === item.answer) entry.classList.add('correct');
        });
        state.answered[String(state.activeCell)] = true;
        const correct = choice === item.answer;
        recordAnswer(item, correct);
        if (correct) {
          state.score += value;
          button.classList.add('correct');
          feedbackTitle.textContent = `Correct. +$${value}`;
        } else {
          button.classList.add('wrong');
          feedbackTitle.textContent = `Not quite. The answer is ${item.answer}.`;
        }
        feedbackCopy.textContent = item.explanation;
        feedback.hidden = false;

        const actions = root.querySelector('.actions');
        actions.innerHTML = '';

        const next = document.createElement('button');
        next.className = 'btn btn-primary';
        next.textContent = Object.keys(state.answered).length === currentQuestions().length ? 'See Score' : 'Back to Board';
        next.addEventListener('click', () => {
          if (Object.keys(state.answered).length === currentQuestions().length) {
            const jeopardyCategories = currentJeopardyCategories();
            const maxScore = jeopardyCategories.reduce((sum, category) => sum + category.indexes.reduce((inner, _, row) => inner + ((row + 1) * 100), 0), 0);
            renderResult('Jeopardy', state.score, maxScore, initJeopardy, 'Nice. You cleared the whole board.');
            return;
          }
          renderJeopardyBoard();
        }, { once: true });
        actions.append(next);
      }

      function findJeopardyRow(index) {
        const jeopardyCategories = currentJeopardyCategories();
        for (const category of jeopardyCategories) {
          const row = category.indexes.indexOf(index);
          if (row !== -1) return row;
        }
        return 0;
      }

      function initStats() {
        state = { mode: 'stats' };
        setActiveMode('stats');
        renderStats();
      }

      function initMissedPractice() {
        const deck = buildMissedQuestions();
        if (!deck.length) {
          initStats();
          return;
        }
        state = { mode: 'missed', deck, index: 0, score: 0, locked: false };
        setActiveMode('stats');
        renderMissedPractice();
      }

      function renderMissedPractice() {
        const item = state.deck[state.index];
        const progress = ((state.index + 1) / state.deck.length) * 100;
        root.innerHTML = `
          <div class="panel-top">
            <div>
              <h2>Redo Missed Questions</h2>
              <p>Question ${state.index + 1} of ${state.deck.length}</p>
            </div>
            <div class="pill">Score ${state.score}</div>
          </div>
          <div class="progress-track"><div class="progress-fill" style="width:${progress}%"></div></div>
          <div class="question">${item.question}</div>
          <div class="choices">
            ${item.choices.map((choice) => `<button class="choice" data-choice="${escapeHtml(choice)}">${choice}</button>`).join('')}
          </div>
          <div class="feedback" id="feedback" hidden>
            <strong id="feedback-title"></strong>
            <p id="feedback-copy"></p>
          </div>
          <div class="actions">
            <button class="btn btn-primary" id="next-btn" hidden>${state.index === state.deck.length - 1 ? 'Back to Stats' : 'Next Question'}</button>
            <button class="btn btn-secondary" id="stats-btn">Back to Stats</button>
          </div>
        `;

        root.querySelectorAll('[data-choice]').forEach((button) => {
          button.addEventListener('click', () => handleMissedChoice(button, button.textContent, item));
        });
        root.querySelector('#stats-btn').addEventListener('click', initStats);
      }

      function handleMissedChoice(button, choice, item) {
        if (state.locked) return;
        state.locked = true;
        const feedback = root.querySelector('#feedback');
        const feedbackTitle = root.querySelector('#feedback-title');
        const feedbackCopy = root.querySelector('#feedback-copy');
        const nextBtn = root.querySelector('#next-btn');
        root.querySelectorAll('[data-choice]').forEach((entry) => {
          entry.disabled = true;
          if (entry.textContent === item.answer) entry.classList.add('correct');
        });
        const correct = choice === item.answer;
        recordAnswer(item, correct);
        if (correct) {
          state.score += 1;
          button.classList.add('correct');
          feedbackTitle.textContent = 'Correct';
        } else {
          button.classList.add('wrong');
          feedbackTitle.textContent = `Not quite. The answer is ${item.answer}.`;
        }
        feedbackCopy.textContent = item.explanation;
        feedback.hidden = false;
        nextBtn.hidden = false;
        nextBtn.addEventListener('click', () => {
          if (state.index === state.deck.length - 1) {
            initStats();
            return;
          }
          state.index += 1;
          state.locked = false;
          renderMissedPractice();
        }, { once: true });
      }

      function renderStats() {
        const unitStats = currentUnitStats();
        const categories = Object.entries(unitStats.categories);
        const missed = missedQuestions();
        root.innerHTML = `
          <div class="panel-top">
            <div>
              <h2>Stats</h2>
              <p>${currentUnit.title}</p>
            </div>
            <div class="pill">${missed.length} missed</div>
          </div>
          <div class="stats-grid">
            ${categories.map(([category, values]) => `
              <div class="stats-card">
                <strong>${category}</strong>
                <div class="stats-percent">${accuracyPercent(values.correct, values.attempts)}</div>
                <p>${values.correct} right out of ${values.attempts} tried</p>
              </div>
            `).join('')}
          </div>
          <div class="stats-missed">
            <div class="panel-top">
              <div>
                <h2>Missed Questions</h2>
                <p>${missed.length ? 'Tap redo to practice the ones you missed.' : 'Nothing missed right now.'}</p>
              </div>
            </div>
            ${missed.length ? `
              <div class="actions">
                <button class="btn btn-primary" id="redo-all-btn">Redo Missed Questions</button>
                <button class="btn btn-secondary" id="reset-stats-btn">Reset Stats</button>
              </div>
              <div class="missed-list">
                ${missed.map((item, index) => `
                  <div class="missed-item">
                    <div>
                      <div class="missed-tag">${item.category}</div>
                      <div class="missed-question">${item.question}</div>
                    </div>
                    <button class="btn btn-secondary" data-redo-index="${index}">Redo</button>
                  </div>
                `).join('')}
              </div>
            ` : `
              <div class="actions">
                <button class="btn btn-secondary" id="reset-stats-btn">Reset Stats</button>
              </div>
            `}
          </div>
        `;

        if (missed.length) {
          root.querySelector('#redo-all-btn').addEventListener('click', initMissedPractice);
          root.querySelectorAll('[data-redo-index]').forEach((button) => {
            button.addEventListener('click', () => {
              const item = missed[Number(button.dataset.redoIndex)];
              state = { mode: 'missed', deck: [{ ...item, choices: shuffle(item.choices) }], index: 0, score: 0, locked: false };
              setActiveMode('stats');
              renderMissedPractice();
            });
          });
        }

        root.querySelector('#reset-stats-btn').addEventListener('click', () => {
          if (!confirmStatsReset()) return;
          stats[currentUnit.id] = createEmptyUnitStats(currentUnit);
          saveStats();
          renderStats();
        });
      }

      function renderResult(label, score, total, restartFn, customCopy) {
        root.innerHTML = `
          <div class="result">
            <div class="panel-top">
              <div>
                <h2>${label}</h2>
                <p>Round complete.</p>
              </div>
              <div class="pill">Finished</div>
            </div>
            <div class="question">You got ${score}${label === 'Jeopardy' ? '' : ` out of ${total}`}</div>
            <p>${customCopy || resultCopy(score, total)}</p>
            <div class="actions" style="justify-content:center;">
              <button class="btn btn-primary" id="restart-final">Play Again</button>
            </div>
          </div>
        `;
        root.querySelector('#restart-final').addEventListener('click', restartFn);
      }

      function resultCopy(score, total) {
        if (score === total) return 'Perfect score. You know this unit cold.';
        if (score >= Math.ceil(total * 0.8)) return 'Very solid. A quick rerun and you are locked in.';
        return 'Good start. Another round will help the tricky details stick.';
      }

      function escapeHtml(value) {
        return value.replaceAll('&', '&amp;').replaceAll('"', '&quot;').replaceAll('<', '&lt;').replaceAll('>', '&gt;');
      }

      unitButtons.forEach((button) => {
        button.addEventListener('click', () => {
          setActiveUnit(button.dataset.unit);
          if (state.mode === 'multiple') initMultiple();
          if (state.mode === 'typing') initTyping();
          if (state.mode === 'millionaire') initMillionaire();
          if (state.mode === 'jeopardy') initJeopardy();
          if (state.mode === 'stats' || state.mode === 'missed') initStats();
        });
      });

      modeButtons.forEach((button) => {
        button.addEventListener('click', () => {
          const mode = button.dataset.mode;
          if (mode === 'multiple') initMultiple();
          if (mode === 'typing') initTyping();
          if (mode === 'millionaire') initMillionaire();
          if (mode === 'jeopardy') initJeopardy();
          if (mode === 'stats') initStats();
        });
      });

    </script>
  </body>
</html>
